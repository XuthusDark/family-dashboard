PORT=3001
FRONTEND_URL=http://localhost:5173
# Set this to your Pi's IP when deployed, e.g. http://192.168.1.50
# FRONTEND_URL=http://192.168.1.50

# Weather location (Open-Meteo uses lat/lon)
WEATHER_LAT=40.7128
WEATHER_LON=-74.0060
WEATHER_TIMEZONE=America/New_York

# Admin PIN (4 digits, stored hashed)
# Set via admin UI or manually: node -e "import('./src/db/index.js').then(m=>m.setPin('1234'))"
